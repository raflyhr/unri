export interface News {
  id: string;
  title: string;
  image: string;
  date: string;
  category: string;
}

export interface Program {
  id: string;
  title: string;
  description: string;
  icon: string;
}
